import static io.println(int);
import ioPlus.printResult(int, boolean) void;

    /* this is a gamer comment tbh */
class test extends testerest {
    int i; int j;
    public int func(boolean a) {
        ah ah; ah ah; staying alive; staying alive;
        ah ah; ah ah; staying alive; staying aliiiiiiiiiiiiive;
        boolean a;
        int a;
        int[] b;
        c;
        d[2][4].oi(a,b,c);
        e[2] = 3;

        // c. fop-sa fl-s ur-a fi-sofc siof ipf soifu
        b = 3 / 8;
        false[true];

        element[new int[6]] = new Object();

        longaceess[4 * 2 + 1 + 9 / 8];

        this.function(this * 4, this / 3);

        while(true) oi; oi;
        if (false && 20 && (0-10+20+20))
           a[1] = 455 + 20 * 10 * 20 / a20[3];
        else true;
        return a;
    }


    public static void main(String[] args) {
          io.println(Hello);
    }
}